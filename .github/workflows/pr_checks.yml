name: PR Validation

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - reopened
jobs:
  pr-validation:
    if: github.actor != 'dependabot[bot]'
    uses: anket-cd/shared-workflows/.github/workflows/pr-checks.yml@v6
    secrets: inherit
    with:
      subjectPattern: '^.+$'