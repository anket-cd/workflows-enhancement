name: PR Validation

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - reopened

jobs:
  pr-validation:
    uses: anket-cd/shared-workflows/.github/workflows/pr-checks.yml@v1
    secrets: inherit