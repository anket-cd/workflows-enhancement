name: PR Validation

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - reopened

jobs:
  pr-validation:
    uses: anket-cd/shared-workflows/.github/workflows/pr_checks.yml@v1.3.0
    secrets: inherit